import React from 'react';

function MenuTitle({ children }) {
  return (
    <h6 className="mb-2 text-sm font-semibold text-gray-400 dark:text-gray-500">{children}</h6>
  );
}

export default MenuTitle;
