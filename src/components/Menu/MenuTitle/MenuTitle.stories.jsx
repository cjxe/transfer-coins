import MenuTitle from '@/components/Menu/MenuTitle/MenuTitle';

export default {
  title: 'Components/Menu/Title',
  component: MenuTitle,
};

export const Base = {
  args: {
    children: 'Public',
  },
};
